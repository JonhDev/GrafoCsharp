<Window x:Class="GrafoUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GrafoUI"
        xmlns:arrows="clr-namespace:Petzold.Media2D;assembly=Arrowheads"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>


        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10">
            <Label Content="ToolBox"/>
            <Separator/>
            <Button Margin="5" Padding="2" x:Name="addVertice" Content="Agregar vertice"/>
            <Button Margin="5" Padding="2"  x:Name="addArista" Content="Agregar arista"/>
        </StackPanel>

        <Canvas x:Name="canvas" Grid.Row="1">
        </Canvas>
        
    </Grid>
</Window>
